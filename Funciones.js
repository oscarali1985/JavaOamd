// para eliminar espacios en blanco
// tambien se puedes usar la funcion de js .trim()==' ' para eliminar los espacios en blancos

function myTrim(x) {
    
    return x.replace(/^\s+|\s+$/gm,'');
  }
  
